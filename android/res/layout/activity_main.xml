<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity" >

    <Button
        android:id="@+id/fake_send_btn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="66dp"
        android:text="Send"
        android:visibility="gone" />

    <Button
        android:id="@+id/add_screen_btn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentRight="true"
        android:layout_alignParentTop="true"
        android:layout_marginRight="16dp"
        android:layout_marginTop="160dp"
        android:text="+" />

    <EditText
        android:id="@+id/screen_id_txt"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBaseline="@+id/add_screen_btn"
        android:layout_alignBottom="@+id/add_screen_btn"
        android:layout_toLeftOf="@+id/add_screen_btn"
        android:ems="10"
        android:hint="Screen ID"
        android:inputType="number"
        android:maxLength="6" />

</RelativeLayout>